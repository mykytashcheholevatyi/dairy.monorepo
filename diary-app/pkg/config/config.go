package config

// Config - структура для хранения конфигурации приложения.
type Config struct {
    DatabaseURL  string // URL подключения к базе данных
    JWTSecret    string // Секрет для генерации JWT токенов
}

// LoadConfig - функция для загрузки конфигурации из файла или переменных окружения.
func LoadConfig() *Config {
    // Загрузка конфигурации из файла конфигурации или переменных окружения
    // Валидация необходимых настроек
    // Возвращение структуры Config с загруженными настройками
}

